# =============================================================
# BQ Viewer Frontend — Nginx Config
# =============================================================

# Port Nginx akan listen (default: 80)
NGINX_PORT=80

# Hostname / address backend API (default: backend)
BACKEND_HOST=backend

# Port backend API (default: 5000)
BACKEND_PORT=5000
